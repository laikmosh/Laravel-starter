#--------------------------------------------------------------------------
# Domain and Port Configuration
#--------------------------------------------------------------------------
#
# These settings control how your application is accessed:
# - DOMAIN: Your application's primary domain
# - PORT: Standard HTTP port
# - SECURE_PORT: Standard HTTPS port
#
# Usage: Used across the application for URL generation and service binding
#--------------------------------------------------------------------------
SCHEME=https://
SUBDOMAIN=sub
DOMAIN=domain.com
PORT=80 
SECURE_PORT=443
INTERNAL_PORT=8000

#--------------------------------------------------------------------------
# Reverb Server vs Client Configuration
#--------------------------------------------------------------------------
#
# There are two distinct sets of Reverb settings:
#
# 1. REVERB_SERVER_HOST / REVERB_SERVER_PORT
# These control where your actual Reverb server instance runs.
# Example: 0.0.0.0:8080 (internal server)
#
# 2. REVERB_HOST / REVERB_PORT
# These tell Laravel where to connect as a client.
# Example: ws.laravel.com:443 (public endpoint)
#
# Usage: Route external traffic (ws.laravel.com:443) to internal (0.0.0.0:8080)
#--------------------------------------------------------------------------
# Server Configuration (internal)
REVERB_SERVER_HOST=0.0.0.0 # Allows connections from any IP
REVERB_SERVER_PORT=8080 # Internal port for the WebSocket server
REVERB_SUBDOMAIN=ws

# Client Configuration (external/public)
REVERB_SCHEME=https # Internal connection can use HTTP
REVERB_HOST="${REVERB_SCHEME}${REVERB_SUBDOMAIN}.${DOMAIN}"  # How other clients will connect
REVERB_PORT=443 # External port (standard HTTPS/WSS port)

# App Credentials
REVERB_APP_ID=
REVERB_APP_KEY=
REVERB_APP_SECRET=

#--------------------------------------------------------------------------
# System Paths and Commands
#--------------------------------------------------------------------------
#
# Define system-level paths and commands:
# - WORKDIR: Main application directory
# - XDG paths: Configuration and data directories
# - SUPERVISOR_PHP_COMMAND: Command to start PHP processes
#
# Usage: These paths are used by the application and its services
#--------------------------------------------------------------------------
WORKDIR="/workspaces/backend"
WWWUSER="1000"
WWWGROUP="1000"
XDG_CONFIG_HOME="${WORKDIR}/config"
XDG_DATA_HOME="${WORKDIR}/data"
SUPERVISOR_PHP_COMMAND="/usr/bin/php -d variables_order=EGPCS artisan octane:start --workers=1"
OCTANE_SERVER=frankenphp